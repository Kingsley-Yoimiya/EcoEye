import 'package:flutter/material.dart';

class ResultGraph extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // 这里是一个占位符，你可以根据需要使用一个图表库来实现具体的图表
    return Center(
      child: Text('Result Graph Placeholder'),
    );
  }
}
